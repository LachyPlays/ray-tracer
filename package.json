{
  "name": "ray-tracer",
  "version": "1.0.0",
  "description": "A simple multithreaded cpu-only ray tracer ",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc public/index.ts --outFile public/index.js && nodemon index.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.14"
  }
}
